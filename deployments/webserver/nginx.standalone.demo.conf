# Generated by the SDKWork web server standard (SDKWORK_WEBSERVER_SPEC.md)
# from server.common.toml + server.standalone.toml; do not edit by hand.
user sdkwork;
worker_processes auto;
pid /run/sdkwork/models/webserver.pid;
error_log /var/log/sdkwork/models/webserver/error.log warn;
events {
    worker_connections 1024;
}
http {
    sendfile on;
    keepalive_timeout 75;
    client_max_body_size 1100m;
    server_tokens off;
    gzip on;
    upstream gateway {
        least_conn;
        keepalive 32;
        server 127.0.0.1:8080;
    }
    server {
        listen 80;
        server_name models-demo.sdkwork.com models-demo.birdcoder.com models-demo.dtupay.com models-demo.noaper.com models-demo.sdkwork.cn models-demo.birdcoder.cn models-demo.dtupay.cn models-demo.noaper.cn models-demo.skubc.com models-demo.skubc.cn models-demo.zowalk.com models-demo.zowalk.cn models-demo.offer86.com models-demo.offer86.cn models-demo.86offer.com models-demo.86offer.cn;
        include snippets/gateway-api-locations.nonproduction.conf;
        location / {
            root /usr/share/sdkwork/models/web/pc;
            index index.html;
            try_files $uri $uri/ /index.html;
        }
    }
}
