# Generated by the SDKWork web server standard (SDKWORK_WEBSERVER_SPEC.md)
# from server.common.toml + server.cloud.toml; do not edit by hand.
user sdkwork;
worker_processes auto;
pid /run/sdkwork/models/webserver.pid;
error_log /var/log/sdkwork/models/webserver/error.log warn;
events {
    worker_connections 1024;
}
http {
    sendfile on;
    keepalive_timeout 75;
    client_max_body_size 1100m;
    server_tokens off;
    gzip on;
    upstream gateway {
        least_conn;
        keepalive 32;
        server sdkwork-api-cloud-gateway:8080;
    }
    server {
        listen 80;
        server_name models-test.sdkwork.com models-test.birdcoder.com models-test.dtupay.com models-test.noaper.com models-test.sdkwork.cn models-test.birdcoder.cn models-test.dtupay.cn models-test.noaper.cn models-test.skubc.com models-test.skubc.cn models-test.zowalk.com models-test.zowalk.cn models-test.offer86.com models-test.offer86.cn models-test.86offer.com models-test.86offer.cn;
        include snippets/gateway-api-locations.nonproduction.conf;
        location / {
            root /usr/share/sdkwork/models/web/pc;
            index index.html;
            try_files $uri $uri/ /index.html;
        }
    }
}
