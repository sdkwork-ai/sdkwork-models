# Generated by the SDKWork web server standard (SDKWORK_WEBSERVER_SPEC.md)
# from server.common.toml + server.standalone.toml; do not edit by hand.
user sdkwork;
worker_processes auto;
pid /run/sdkwork/models/webserver.pid;
error_log /var/log/sdkwork/models/webserver/error.log warn;
events {
    worker_connections 1024;
}
http {
    sendfile on;
    keepalive_timeout 75;
    client_max_body_size 1100m;
    server_tokens off;
    gzip on;
    upstream gateway {
        least_conn;
        keepalive 32;
        server 127.0.0.1:8080;
    }
    server {
        listen 80;
        server_name models-dev.sdkwork.com models-dev.birdcoder.com models-dev.dtupay.com models-dev.noaper.com models-dev.sdkwork.cn models-dev.birdcoder.cn models-dev.dtupay.cn models-dev.noaper.cn models-dev.skubc.com models-dev.skubc.cn models-dev.zowalk.com models-dev.zowalk.cn models-dev.offer86.com models-dev.offer86.cn models-dev.86offer.com models-dev.86offer.cn;
        include snippets/gateway-api-locations.nonproduction.conf;
        location / {
            root /usr/share/sdkwork/models/web/pc;
            index index.html;
            try_files $uri $uri/ /index.html;
        }
    }
}
